ty Filter
    iter: i32
    pred: u8

    id.& = i

    getIter.& = i.iter
    getPred.& = i.pred

    getIter2.* i: Y = i.getIter

ty A = Yes | No
    id.& = i

ty optional = None | Some A
    id.& = i

ty A2
    Yes
    No

    id.& = i

ty optional2
    None
    Some A2

    id.& = i
